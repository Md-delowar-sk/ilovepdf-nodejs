const express = require('express');
    const app = express();
    app.get('/api/data', (req, res) => {
      res.json({ message: "ডেটা প্রসেসিং সম্পন্ন!" });
    });
    app.listen(3000, () => console.log('সার্ভার চালু হয়েছে'));
